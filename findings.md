# 发现与决策

## 2026-06-02 · 生成期质量闸（三梯队）

### F9 · 系统测错了极性：全在防"别漂移"，缺"好不好看"
- **现象**：盘点全部工具——一致性底账、语言指纹、合规审计——**清一色负向防护**（别写崩/别用词烂/别跑味）。决定一本书成败的"好不好看"被整个外包给"将来人工 + 真实读者"，生成期零 outcome 信号。
- **根因**：负向防护好量化（漂移有基线），正向质量难量化（无基线、LLM 自评偏高），于是只做了好做的那半。
- **行动**：补"每章质量闸"。原则 **先有尺子再添功能**——没有逐章 outcome 信号，加任何新引擎都没法判断有没有用（连消融都跑不了）。
- **适用范围**：任何生成式质量系统都有这个引力——往可量化的负向指标堆，回避难量化的正向目标。要主动纠偏。

### F10 · "好不好看"测不了，但"弃书的硬伤"可枚举——证伪绕开自评偏高
- **现象/解法**：不让 LLM 给质量打分（它总报高）。改成**对抗 skeptic 猎缺陷**：每个盯一类失败模式（说明文腔/情绪外露/上帝剧透/注水/OOC/爆点落空/套路疲劳），要求**引出原句**证明"有一条读者会划走的理由"，找不到才算过。
- **根因**：**证伪比证好可靠**（refutation 的不对称性）。证"好"无成本无证据；证"有具体硬伤"必须落到原句，自评偏高被这个不对称挡在门外。
- **关键区分**：这**不是读者模拟**——不立 persona、不猜喜好，只抓有原句证据的硬伤。所以它不踩"persona ≠ 真读者"那条坑（与读者信号口"不做模拟"一致）。
- **配套·验收单（TDD 化）**：另一半是正向——生成前从 ④关键节拍+⑥到期伏笔立"本章验收单"，生成后验交付。猎缺陷（负向硬伤）+ 验收单（正向交付）+ 合规（契约一致）= 逐章质量闸。
- **优先级判断**：质量闸（最该先做、最便宜、是其余一切的标尺）＞ 框架消融（需标尺）＞ 结构指纹（最创新但晚做，门槛：过 20 章+消融证明结构有用）。**最巧妙 ≠ 最该先做。** 详见 `docs/notes/2026-06-02-生成期质量闸-路线图.md`。

## 2026-06-02 · F6/F7 固化进 skill（review 补强）

### F8 · "已验证技法只活在 findings"本身是缺口
- **现象**：上轮 review 确认六层 + runtime 框架已完整，但 F6（样例锚）/F7（改写 pass）这两个 n=1 验证有效的技法只写在 findings，没进该执行它们的 skill；且 runtime tick 第 2 步还是"跑味重 roll"，与 F7 结论（改写收敛、不重 roll）相反、也与第 3 步指纹的"改写"自相矛盾。
- **根因**：findings 是发现层、skill 是执行层；发现没回流到执行，等于没落地。
- **行动**：样例锚 → `novel-style` 模式二；改写 pass → `novel-runtime` tick 第 2 步 + 装配加样例锚块；编排文档阶段三同步。
- **适用范围**：任何"先实验后建框架"的流程——实验结论必须显式回流进 SOP，否则下次执行仍走模型默认老路。

### F3 续 · 指纹硬验证此前不可复现，已补合成 fixture
- 原 `fixtures/` 只有 names.txt，验证用的 3 章真实正文带版权未提交 → progress 里"硬验证通过"在仓库内复现不了。
- 补 3 章合成正文（原创，埋 `不动声色`/`压不下去`），实跑退出码 2、surface 两 tic——硬验证现在仓库内可复现，且充当回归锚。诚实边界不变：主题层趋同仍交 ⑤ 审计兜底。

## 2026-06-01 · 小说运行时层

### F1 · 框架优化错了那 1%（核心诊断）
- **现象**：六层准备包很精致，但唯一实测坏掉的是语言劣化（运行时），且所有缺口都在运行时；验证只做到第 1 章（最易处）。
- **根因**：一本书 99% 工作量在连载期"写第 N 章"，但 effort 全压在 design-time 静态契约上，方向反了。
- **适用范围**：任何长文 AI 生成；"静态契约"管不住"跨章累积的动态漂移"。
- **行动**：补 `novel-runtime` 运行时控制层；技术最缺的（书尺度运行时一致性）正好=阅文抄不动的差异化楔子。

### F2 · 四个运行时 store 不能合并成一张表
- **现象**：想把"角色状态/名词/伏笔/语言指纹"统一成一个状态机，扒下去发现是四种数据结构（寄存器/append-only/带TTL工单/滑窗频率），失效模式各异。
- **根因**：统一的是**控制环（tick: extract→commit→feed-forward）**，不是数据——像 ECS 的逐帧更新，可复用的是接口不是表。强行合表=假抽象。
- **延伸**：四 store 落在"机械↔人工"光谱两端——语言指纹纯计算无需确认（daemon），伏笔靠作者意图 LLM 抽不出（只提醒）。所以不能用同一种自动化对待。

### F3 · 中文纯计算指纹的能力边界（验证得出）
- **现象**：字符 n-gram 计数 ①被人名噪声淹没 ②章末主题趋同（同母题换词）字面余弦测不出。
- **根因**：无分词 + 主题相似 ≠ 字面相似。
- **解法**：①喂 ⑥ 名词表当 stoplist 滤专名（daemon 消费 ⑥ 的干净接口）；②主题层诚实交 ⑤ LLM 审计兜底——这也给"审计虽只验 conformance 但不可省"一个具体理由。
- **结论**：daemon 定位是高召回"烟雾报警器"，下游人工/审计分诊，不是法官。

### F4 · 审计验的是 conformance 不是 outcome
- 八维审计只检查"符不符合自己定的契约"，**不**检查"好不好看"。八维全绿 ≠ 好看，是 lint 过 ≠ 功能对。质量信号要等人工通读 + 真实读者数据。这是 A4 降级的依据。

### F5 · 维度绑定测量（A5，已跑·n=1 假设级）
- **方法**：固定一个微观 beat（韩立摸到小瓶），只翻转一维、其余锁死，重生成看输出是否独有变化。
- **现象/结论**：7 个刻度维度 A–G **全部绑定，无死重，一个不删**。
  - 强绑定·独立：B 主角 / D 语言 / F 世界温度（同一 beat 翻转立即可见独有变化）。
  - 绑定但**冗余**：A 视角剧透子轴 × G 信息释放 × 禁忌"不剧透"条**三重管"信息差/剧透"**——唯一可简化点，方向是合并分工不是删。
  - C 节奏 / E 冲突在微观 beat **测不出**（独处+短段无从挂载），章级骨架对照确认其实强绑定。
- **根因/教训**：绑定测试**必须按维度作用尺度选输出长度**——B/D/F 一个 beat 够，A/G 一段，**C 必整章、E 必多人场景**。原 spec"只改一维重生成"若在 beat 级跑会误判 C/E 为零绑定。
- **局限**：单模型自评、单书单场景，suggestive 非定论；多样本 + 盲评才能定。
- **未执行**：三重剧透冗余的合并（动框架轴，待用户确认）。

### F6 · 样例锚 + 改写 pass 提升 ch1 达标率（已 demo·n=1）
- **问题**：抽象契约（9 行维度散文）注入后，ch1 仍跑偏——因为它是**方向不是约束**，钉不住词/句级，模型默认拉力（长句/景物抒情/上帝剧透/情绪外露）畅通无阻。
- **demo**：同一 ch1 beat + 同契约，三臂对照。A 抽象一遍过 5/6 指标不达标；B 加 2 段冷峻样板一遍过全达标；C 对 A 做"只改纹理不动情节"的改写 pass，也拉达标。
- **结论**：①**样例锚**最省力（模型模仿具体文本 >> 遵守抽象取值）；②**改写 pass** 兜底且收敛（不重 roll）；③ch1 锚用参照书片段，定稿后自身成为 ch2+ 的锚（自举）。
- **顺带证 F4 延伸**：判分指标里**句长/最长句/对话占比纯机器可测**（当场判死），景物/剧透/外露/试金石才是语义——审计一半可机械化。
- **局限**：n=1、自评、基线被我有意放真实漂移；属手段有效性演示，非统计结论。可测指标用户可自核。
- **下一步候选**：①工程化 `novel-stylecheck` lint；②把"样例锚+改写pass"写进 novel-style/novel-runtime 注入流程。

### F7 · 一章 agent 团队跑通（workflow·5 agent）
- **结构**：写手 → 对抗审计组(风格/一致性/重复，parallel) → 改写；人工 gate 不交 agent。
- **核心战果**：**独立一致性审计抓到写手自造的真 bug**——药园管事写成"墨大叔"，底账登记是"张老头"。这是"写手≠审计"非分不可的实锤（自审发现不了）。
- **机械集成验证**：重复审计 agent **真调用了 `fingerprint.mjs`**（Write+Bash），活团队里跑通了 daemon。
- **改写收敛**：每处改写都追溯得到某条审计（格言句去除、翻来覆去→反复盘算、不起眼→不打眼），情节零改动——是消费审计清单，不是重 roll。
- **红线验证**：改写**故意没动"墨大叔"**，因为"改设定还是笔误"是创作决定→正确地停在人工 gate。"agent 干活、人把关"成立。
- **诚实边界**：样例锚 + opus 写手使第一稿就较干净，ch1 非劣化重灾区；团队这次的价值在抓一致性 bug 而非救文笔。多 agent ~10 万 token/章，成本真实。

## 现有提交记录

| 提交 | 类型 | 内容 | 已有文档 |
|------|------|------|---------|
| f571bda | init | first commit | - |
| 486ae36 | feat | monorepo Phase 0 scaffold | 设计 + 计划 |
| 1675c7f | chore | CLAUDE.md 结构优化 | - |
| 75d59e3 | feat | knowledge-feed 工具 | 设计 + 计划 |
| 2d64077 | refactor | knowledge-feed 常量提取 | - |
| (uncommitted) | feat | news-curator 实验 | README only |

## 文档缺口

- news-curator：有 README，缺设计文档和实现计划
- 根 README：未提及 knowledge-feed 和 news-curator
