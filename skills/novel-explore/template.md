# 发散决策记录 · 框架模板（Explore Decisions Log）

> 这是**框架**（决策记录的 schema），不是某本书的内容。
> explore 每做一次 bake-off + 选择，就 **append 一条**。这是**纯归档流水账**——记"选了什么 / 为什么 / 落到哪"，**runtime 不读它**（选中的锚已经写进各层实例文件 / 装配样例锚块了，这里只留账）。
> 用法：复制到 `docs/explore/<作品名>-decisions.md`，每次发散后追加一条。删除本提示块。

---

## 0. 元信息

| 字段 | 内容 |
|---|---|
| 作品名 / 工作名 | |
| 所属气质 | `<对应哪份风格契约>` |

---

## 决策流水（每次发散 append 一条 · 时间正序，新的在下）

> 每条至少七项：日期 / 档位 / 分叉轴 / 候选列表 / 选中项 / 选择理由 / 落盘去向。  
> 档位若是 B 质感锚或带探针，补"探针记录"。

### `<YYYY-MM-DD>` · `<档位名>`

- **决策点**：`<在选什么>`
- **分叉轴**：`<这次沿哪条轴铺版本——含金量所在>`
- **候选**：
  - A：`<一句话+关键差异>`
  - B：`<一句话+关键差异>`
  - C：`<一句话+关键差异>`
- **选中**：`<哪个 / 或混搭说明（如"B 的机制 + A 的代价设定"）>`
- **理由**：`<为什么选它——你识别到的具体差异>`
- **落盘去向**：`<锚类型 + 落到哪个文件的哪段>`
  - 例（A 方向锚）：`①concept docs/concepts/<书>.md §3 机制 + §4 强度曲线，§5 龙头接口已校`
  - 例（B 质感锚）：`runtime 装配·样例锚块，顶替 ch1 默认参照片段`
  - 例（C 卖相锚）：`本表 append（不进装配）`
- **探针记录**（可选）：`<如 #11：本次对照"多版本选 vs 写一版+反应"的观察>`

---

> **schema 约束**：不加字段就够用别加；落盘去向**必须落到具体文件的具体段/块**，不能停在"⑤ 风格层"这种抽象层名（集成空头的教训，见 findings F11/F13）。
