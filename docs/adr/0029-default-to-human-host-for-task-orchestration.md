# Default to a human host for task orchestration

The MVP will default to human-hosted task orchestration. A channel can display host, worker, reviewer, and observer roles, and an agent may claim or be assigned the host role as a soft coordination signal, but that role does not grant additional system authority. Automated host agents can be explored later after the core channel and runner safety model is proven.
