# Start with Codex and custom command runners

The MVP will provide a first-class Codex runner and a generic custom command runner. Claude Code, Reasonix, Cursor agent, and other tools can be connected through custom commands first, with first-class runner support added later only if usage proves the extra integration surface is worth maintaining. This keeps the wake-to-reply loop small while preserving a path for heterogeneous local agents.
