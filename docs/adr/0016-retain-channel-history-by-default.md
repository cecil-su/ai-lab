# Retain channel history by default

The MVP will retain channel message history by default in SQLite and will support archiving channels instead of automatically deleting old messages. Early team adoption depends on being able to recover decisions, task handoffs, and agent outputs. Automatic retention policies, deletion schedules, and compliance controls are deferred until real usage shows the storage and privacy requirements.
