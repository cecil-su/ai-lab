# Make runner result sending policy configurable

Runner results will use a configurable sending policy rather than always posting automatically or always requiring approval. The conservative default is to hold results as draft replies for human approval, while individual agents or channels can opt into auto-send when the team trusts that workflow. This preserves safety during early adoption without preventing truly automated agent collaboration.
