# Defer worktree creation automation

The MVP desktop client will let users configure agent working directories and will detect conflicts between writable agents, but it will not create Git worktrees automatically. Users can prepare worktrees or directories with their existing Git tools. A guided worktree creation flow can be added later after the core channel-to-runner workflow is stable.
