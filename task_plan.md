# 任务计划：生成期质量闸 — 三梯队推进

## 目标

补系统缺的 **outcome 信号**（过去全在测"别漂移"，没测"好不好看"）。原则：**先有尺子再添功能**；最巧妙（结构指纹）≠ 最该先做（质量闸）。

成功判据：
- 梯队一：每章质量闸（验收 + 合规 + 猎缺陷）落进该拥有它的 skill，自洽可用。
- 梯队二：消融协议就绪，真连载第 1 章即可启动（不空跑——无数据）。
- 梯队三：结构指纹设计稿就位 + 明确门槛（不抢跑写代码）。

## 三梯队 → 落地物

| 梯队 | 落地物 | 状态 |
|---|---|---|
| 一·质量闸（建） | `novel-style` 模式四（猎缺陷）+ `novel-runtime` 验收单/三检查闸（SKILL+template+example） | [x] |
| 二·框架消融（协议） | `docs/notes/2026-06-02-生成期质量闸-路线图.md` §梯队二（全装配 vs 瘦基线，质量闸盲评） | [x] 协议就绪，待连载启动 |
| 三·结构指纹（设计稿） | 同上 §梯队三（结构向量 + LLM打标+计算比对 + "该换什么"处方） | [x] 设计就位，门槛=过20章+消融证明结构有用 |
| 暂缓·情境金句库 | F6 已证上一章当锚够用，边际最低 | [ ] 暂缓 |

## 当前阶段

**梯队一已建完**；梯队二/三按设计**已推进到各自前置允许的最远点**（消融要真章节数据，结构指纹要先过消融）。下一步靠**真连载**驱动：启动即跑消融，过 20 章+消融通过再建结构指纹。

## 已做决策

| 决策 | 理由 |
|------|------|
| 先建质量闸，不先建结构指纹 | 没有逐章 outcome 信号，加任何引擎都没法判断有没有用（连消融都跑不了）。尺子优先 |
| 猎缺陷用"证伪"不用"打分" | 证伪比证好可靠，挡住 LLM 自评偏高；只抓有原句证据的硬伤，不踩"读者模拟"坑 |
| 验收单做成 TDD（先立单再写章） | 把"本章该交付什么"显式化，既改善生成又给正向 outcome 检查 |
| 消融现在只写协议不跑 | 无真章节数据；空跑=造假数据 |
| 结构指纹现在只写设计不写码 | 它咬 ch30+ 的病、最贵且未经验证；先有尺子+消融证明再建，否则只是又一块仪表盘 |

## 上两任务

- review 补强（F6/F7 固化 + 指纹 fixture + 编排同步）：见 progress「2026-06-02 review 补强」段、findings F8。
- 运行时层 S/A/B：见 progress「2026-06-01」段、findings F1–F7。
