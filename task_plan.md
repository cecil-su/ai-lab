# 任务计划：novel-explore 发散前端 skill

## 目标

补现有七 skill 缺的**发散上游** —— "AI 出多版本 → 人选 → 选中的当后续写作锚"。现有体系全是**收敛**（每层逼向一个答案）+ **负向防护**（别漂移/别写崩/猎缺陷），假设你已知道要什么；explore 服务"我有品味但说不清、还在选方向"的早期阶段。

设计单一来源：`docs/designs/2026-06-02-novel-explore.md`。

成功判据：
- 一套**高度无关的发散协议** + **可加行的档位登记表**（加档位=加行，协议不动）。
- 协议跨**两种锚**成立：质感锚（试写片段→⑤样例锚）+ 方向锚（金手指→①concept）。
- 铁律守住：AI 只发散、绝不排名/推荐；人决策。
- 选择**落盘**成 `docs/explore/<书>-decisions.md`，runtime 装配读得到。

## 核心设计（详见设计文档）

| 维度 | 结论 |
|---|---|
| 形态 | 独立 `novel-explore` skill（不在现有 skill 各加发散模式——避免同一逻辑重复 5 次） |
| 三种锚 | A 方向锚（→①②③④）/ B 质感锚（→⑤样例锚）/ C 卖相锚（→决策记录，补搁置的⑦市场层） |
| 引擎 | 五步发散协议（逼出分叉轴→并行铺3-4版→点差异→人选→落盘）+ 档位登记表 |
| 连载期 | 不只 t=0：拐点重试质感锚 / 卡文开多版本 / 结构指纹"套路疲劳"处方→人选→喂回 runtime |

## 建造顺序

| 阶段 | 落地物 | 状态 |
|---|---|---|
| 0·设计 | `docs/designs/2026-06-02-novel-explore.md` | [x] 已立 + **两轮 agent-teams 评审（1轮3视角 + 2轮2视角）+ 修订到 v0.3** |
| 1 | 发散协议 + 档位表骨架（含"落盘动作"列）+ **开局策略 + 开篇场景质感试写（合流）** + 金手指/logline + decisions 模板 | [ ] 待用户拍板启动 |
| 2 | 补 A 类（主角/分卷/感情线/爽点配方[改]）+ 新增（断章钩/金手指首次兑现/卷末爆点） | [ ] |
| 3 | 补 C 类卖相锚（书名/简介）+ B 类其余（视角/命名调性/口癖/名场面，多为"存疑·待汰"） | [ ] |
| 4 | 接 runtime（套路疲劳处方 + tick 新增"处方接入"步骤；当下先接 ⑤ 猎缺陷人工条） | [ ] |

## 当前阶段

**设计已两轮评审（1轮 架构/证伪/网文 + 2轮 回归/一致性）+ 修订到 v0.3，skill 未建。** 等用户拍板进入阶段 1。

**战略定位（评审后定）**：explore 与质量闸消融是**上下游、非正交**——开写前帮选方向且不沉淀 runtime 的档位可独立先行；喂回 runtime 的档位（质感锚/大纲）依赖消融结论（若某下游层被判仪式，对应档位作废）。

## 已做决策

| 决策 | 理由 |
|------|------|
| 独立 skill，不给现有 skill 各加发散模式 | 同一套"出选项+点差异+选+落盘"逻辑会在 5 个 skill 重复；独立 skill 一处实现，匹配"一个头脑风暴工具"心智 |
| AI 只发散、绝不排名 | 同项目铁律（质量闸不让 AI 评好坏、读者口不做模拟）；人决策是红线 |
| 发散沿"分叉轴"强制分叉 | naive"给三版"会得到换皮近义物；价值在张成空间（同 workflow diverse-lens） |
| 不重造样例锚 | ⑤+runtime 的"消费端"已建；explore 只产锚喂进去 |
| 阶段 1 改为"开局策略 + 开篇场景质感试写（合流）" | 网文评审：开局是第一生死线（前 3 章赶走读者无法挽回，金手指选错能改）；合流让"质感锚探针"与"开局优先"一举两得 |
| 战略选 (c)：照常推进 + 改"正交"为"上下游" | 证伪评审揭穿"正交"是障眼法（explore 喂回的层正是消融要审的层）；但 explore 服务的"选方向"阶段更上游、且消融卡在没有真连载数据，故不缓建，改为诚实标注依赖 |
| 档位一个都不砍（低价值标"存疑·待汰"） | 用户定；遵循项目"未实测不删"原则（同 ⑤ 维度绑定审计），等消融/实战量出确是仪式再汰 |

## 风险

- **最大风险=分叉轴提炼**：提不出真轴就退化成换皮。无银弹，阶段 1 就要拿真案例压测协议第 1 步。
- 多版本生成成本（N× token，质感试写尤贵）；workflow parallel vs skill 内顺序，建造时定。

## 上两任务

- **质量闸三梯队**（前一任务，**parked 非放弃**）：梯队一已建完；梯队二/三按设计推到前置允许的最远点，**待真连载启动**才能跑消融/建结构指纹。见 progress「2026-06-02 三梯队推进」段、`docs/notes/2026-06-02-生成期质量闸-路线图.md`。explore 与它正交，可独立推进。
- review 补强（F6/F7 固化）：见 progress「2026-06-02 review 补强」段、findings F8。
